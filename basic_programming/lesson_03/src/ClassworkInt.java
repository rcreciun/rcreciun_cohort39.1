public class ClassworkInt {
    public static void main(String[] args) {
        // Пример 1: Ожидаемый результат: 30
        int x = 0;
        // x = 10 + 12 / 2 * 4;
        // x = 10 + 20 - 60 / 10;
        // x = (10 + 20) * 3 - 40;
        // x = 10 + 10 * 2;
        System.out.println("Результат 1: " + x);

        // Пример 2: Ожидаемый результат: 16
        int y = 0;
        // y = 4 * 4 + 2;
        // y = (8 + 8) / 2;
        // y = 4 + 4 * 3;
        // y = 64 / 4 - 4;
        System.out.println("Результат 2: " + y);

        // Пример 3: Ожидаемый результат: 3
        int z = 0;
        // z = 25 / 10 + 5;
        // z = 36 / 12 + 1;
        // z = 30 / 10 - 2;
        // z = 9 - 6 / 3;
        System.out.println("Результат 3: " + z);

        // Пример 4: Ожидаемый результат: 12
        int a = 0;
        // a = 2 * 6 / 3;
        // a = 24 / 4 + 4;
        // a = 3 * 4;
        // a = 60 / (5 + 3);
        System.out.println("Результат 4: " + a);

        // Пример 5: Ожидаемый результат: 50
        int b = 0;
        // b = 5 * 10;
        // b = 100 / 2;
        // b = 25 * 2 - 20 / 2;
        // b = 60 + 40 / 2;
        System.out.println("Результат 5: " + b);

        // Пример 6: Ожидаемый результат: 9
        int c = 0;
        // c = 18 / 2;
        // c = 3 * 3;
        // c = 12 - 3;
        // c = 10 - 1;
        System.out.println("Результат 6: " + c);

        // Пример 7: Ожидаемый результат: 20
        int d = 0;
        // d = 40 / 2;
        // d = 10 * 2;
        // d = 60 / 3;
        // d = 30 - 10;
        System.out.println("Результат 7: " + d);

        // Пример 8: Ожидаемый результат: 6
        int e = 0;
        // e = 3 + 3;
        // e = 12 / 2;
        // e = 18 / 3;
        // e = 24 / 4;
        System.out.println("Результат 8: " + e);

        // Пример 9: Ожидаемый результат: 8
        int f = 0;
        // f = 16 / 2;
        // f = 4 * 2;
        // f = 24 / 3;
        // f = 32 / 4;
        System.out.println("Результат 9: " + f);

        // Пример 10: Ожидаемый результат: 7
        int g = 0;
        // g = 14 / 2;
        // g = 21 / 3;
        // g = 28 / 4;
        // g = 35 / 5;
        System.out.println("Результат 10: " + g);
    }
}
