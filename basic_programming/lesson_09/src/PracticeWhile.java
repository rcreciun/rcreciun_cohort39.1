import java.util.Scanner;
import java.util.Random;

/**
 * Цель Задачи: Создать интерактивную игру, где пользователь должен угадать загаданное программой число.
 * <p>
 * Описание Задачи: Программа генерирует случайное число в заданном диапазоне, а пользователь пытается его угадать.
 * После каждой попытки программа должна сообщать, больше ли или меньше введенное число, чем загаданное.
 * <p>
 * #### Шаги Решения:
 * 1. Используйте `Math.random()` или `Random` для генерации случайного числа в заданном диапазоне,
 * например, от 1 до 100.
 * 2. В цикле `while` запрашивайте у пользователя ввод числа.
 * 3. Если пользователь вводит число, которое равно загаданному, используйте `break` для выхода из цикла.
 * 4. Если введенное число больше или меньше загаданного, сообщите об этом пользователю и продолжите цикл.
 * 5. Предусмотрите возможность выхода из игры по специальной команде (например, ввод -1).
 * <p>
 * #### Пример Ввода и Вывода Программы:
 */

public class PracticeWhile {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        Random random = new Random();
        int randomNumber = random.nextInt(100) + 1; // Генерация случайного числа от 1 до 100
        int numberOfAttempts = 0;

        System.out.println("Я загадал число от 1 до 100. Попробуй угадать!");

        // ваш код тут

    }
}
